#include <stdio.h>

int main(void)
{
    int a = 1,b = 1;
    //それぞれ+,-,*,/で四則演算ができる
    printf("a + b = %d\n", a + b);
    printf("a - b = %d\n", a - b);
    printf("a * b = %d\n", a * b);
    printf("a / b = %d\n", a / b);

    return 0;
}